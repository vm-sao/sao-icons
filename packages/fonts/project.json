{
  "name": "fonts",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/fonts",
  "projectType": "library",
  "tags": ["sao-icons", "fonts"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "rm -rf dist/packages/fonts",
          "mkdir -p dist/packages/fonts",
          "node packages/fonts/main.mjs"
        ],
        "parallel": false
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "cd dist/packages/fonts && pnpm publish --access public --no-git-checks"
        ],
        "parallel": false
      }
    },
    "auto": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "pnpm nx build fonts --parallel=false",
          "pnpm nx publish fonts --parallel=false"
        ],
        "parallel": false
      }
    },
    "ci": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "pnpm nx build fonts --parallel=false",
          "pnpm nx publish fonts --parallel=false --dry-run"
        ],
        "parallel": false
      }
    }
  }
}
